import 'package:flutter/material.dart';

class PatientAdsScreen extends StatelessWidget {
  const PatientAdsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("اعلانات"),);
  }
}
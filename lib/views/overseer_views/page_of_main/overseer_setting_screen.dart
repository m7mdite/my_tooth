import 'package:flutter/widgets.dart';

class OverseerSettingScreen extends StatelessWidget {
  const OverseerSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
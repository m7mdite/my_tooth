import 'package:flutter/widgets.dart';

class OverseerRequestScreen extends StatelessWidget {
  const OverseerRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
import 'package:flutter/widgets.dart';

class OverseerProfileScreen extends StatelessWidget {
  const OverseerProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}